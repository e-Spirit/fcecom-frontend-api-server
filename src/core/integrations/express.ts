// @ts-ignore Integration is written in JS and therefore doesn't provide useful type information
import expressIntegration from 'fsxa-api/dist/lib/integrations/express';

export default expressIntegration;
